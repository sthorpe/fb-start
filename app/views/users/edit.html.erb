<br/>
  <h2> My Account </h2>
  <% form_for(current_user) do |f| %>
    <%= f.error_messages %>
    <p>
      <br/>    
      <label for="occ">User's Name</label>
      <%= f.text_field :name %>
    </p>
    <p>
      <label for="occ">Email</label>
  		<%= f.text_field :email %>
  	</p>
  	<p>
      <label for="occ">Gender</label>
  		<%= f.text_field :email %>
  	</p>
  	<p>
      <label for="occ">Location</label>
  		<%= f.text_field :email %>
  	</p>
    <p>
      <%= f.submit 'Update' %>
    </p>
  <% end %>
<br/>